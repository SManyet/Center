import React from 'react';

import { AppBar, Toolbar } from '@material-ui/core';

const Nav = () => (
  <AppBar position="static">
    <Toolbar>
      <h1>benis</h1>
    </Toolbar>
  </AppBar>
);

export default Nav;
